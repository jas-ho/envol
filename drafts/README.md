# drafts

Design drafts for the homepage. Not deployed: Cloudflare Pages serves only `public/`.

- `design.html`: layout and typography draft (single column, pen-and-ink crane). **Mostly stub content.** The Writing, Talks and most Research entries are invented placeholders used to judge the layout; they are not real publications, talks or hearings. The page is an HTML fragment meant to be published as a claude.ai artifact, which adds the `<html>`/`<head>` wrapper.
